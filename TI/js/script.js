const startBtn = document.getElementById('start-btn');

startBtn.addEventListener('click', () => {
    window.location.href = 'preguntas.html'; // Cambia 'preguntas.html' por el nombre de tu archivo de preguntas
});
